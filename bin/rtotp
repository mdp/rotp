#!/usr/bin/env ruby
# -*- encoding: utf-8 -*-

$: << File.expand_path('../../lib', __FILE__)
require 'rotp'

secret = ARGV.shift.to_s.strip

if secret == ''
  puts
  puts "  USAGE: #{File.basename(__FILE__)} SECRET"
  puts
else
  puts ROTP::TOTP.new(secret).now
end
